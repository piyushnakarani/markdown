/** Default demo Markdown for the live preview tool (kept out of i18n to avoid ICU `{var}` parsing). */
export const LIVE_PREVIEW_DEFAULT_MARKDOWN = `# Markdown Live Preview

Type **Markdown** on the left and see rendered output on the right — instantly.

## Why use live preview?

- See headings, lists, and tables as you type
- Preview **code blocks** with syntax highlighting
- Export to PDF when you are ready

### Code example

\`\`\`javascript
function greet(name) {
  return \`Hello, \${name}!\`;
}
\`\`\`

### Table example

| Feature | Supported |
| --- | --- |
| Live preview | Yes |
| Sync scroll | Yes |
| Dark mode | Yes |

> Your content stays in the browser. Nothing is uploaded.
`;
